THORX — REPOSITORY OPTIMIZATION & IMPORT RECOVERY DIRECTIVE

Objective:
You are now acting as THORX Repository Recovery Engineer.

Your previous forensic report concluded that the primary cause of GitHub import failures and excessive Agent quota consumption is repository context bloat (audits, screenshots, prompts, archives, historical artifacts), NOT application complexity.

I want you to perform a COMPLETE implementation plan and recovery strategy.

MISSION:

1. Design the optimal repository architecture for THORX.
2. Produce a ZERO-DOWNTIME migration plan.
3. Ensure future GitHub imports consume minimum possible Agent quota.
4. Ensure NO runtime functionality is lost.
5. Ensure all current Replit functionality remains intact.

--------------------------------------------------
PHASE 1 — DEEP CLASSIFICATION
--------------------------------------------------

Classify EVERY tracked file into one of these categories:

- Runtime Critical
- Build Critical
- Development Only
- Historical Archive
- Audit/Report
- Screenshots
- Prompt/Transcript
- Agent Memory
- Future Feature Stub
- Safe To Remove
- Safe To Move

Generate:

- Total files per category
- Total size per category
- Import impact score
- Runtime dependency score

--------------------------------------------------
PHASE 2 — DEPENDENCY VERIFICATION
--------------------------------------------------

For EVERY file proposed for removal/migration:

Verify:

- Imported anywhere?
- Referenced by package.json?
- Used in Vite?
- Used in Express?
- Used by WebSocket?
- Used by tests?
- Used by migrations?
- Used by Replit workflow?
- Used in production build?

DO NOT ASSUME.

Perform grep-level verification.

--------------------------------------------------
PHASE 3 — IMPORT OPTIMIZATION SIMULATION
--------------------------------------------------

Simulate these scenarios:

Scenario A:
Current repository.

Scenario B:
Remove attached_assets.

Scenario C:
Remove .agent + .agents.

Scenario D:
Remove all audits/reports.

Scenario E:
Minimal thorx-app.

Scenario F:
thorx-app + thorx-docs.

For each scenario estimate:

- File count
- Text payload
- Relative context pressure
- Expected import performance
- Expected Agent quota savings

--------------------------------------------------
PHASE 4 — BUILD A PERFECT THORX-APP
--------------------------------------------------

Design the ideal production repository:

thorx-app/
- client/
- server/
- shared/
- migrations/
- scripts/
- tests/
- configs
- README
- replit.md

Verify:

- npm install
- npm run check
- npm run build
- npm test
- Application boot
- Port 5000

Everything MUST pass.

--------------------------------------------------
PHASE 5 — IMPORT STRESS TEST
--------------------------------------------------

Estimate:

- Current import cost.
- Post-cleanup import cost.
- Percentage reduction.
- Estimated improvement.

Determine:

- Would a fresh Replit import likely succeed?
- Would Agent context initialization improve?
- Would quota usage likely decrease?

Provide confidence level:

- Low
- Medium
- High

--------------------------------------------------
PHASE 6 — FUTURE-PROOFING
--------------------------------------------------

Generate:

1. Improved .gitignore
2. Suggested .dockerignore
3. Suggested .npmignore
4. Repository hygiene rules
5. CI checks preventing archive commits
6. Maximum repository thresholds:

- Max tracked files
- Max markdown files
- Max screenshots
- Max text payload
- Max repository size

--------------------------------------------------
PHASE 7 — FINAL CERTIFICATION
--------------------------------------------------

Answer ALL questions:

1. Can THORX safely become a thorx-app repository?
2. Will any feature break?
3. Will Replit imports likely become reliable again?
4. What percentage of the current repository is non-runtime content?
5. What is the biggest import offender?
6. What is the safest migration strategy?
7. Is repository splitting mandatory or recommended?
8. Is there ANY remaining import bottleneck?

--------------------------------------------------
FINAL OUTPUT REQUIRED
--------------------------------------------------

Produce:

1. Complete migration blueprint.
2. Repository cleanup checklist.
3. Import optimization report.
4. Risk assessment.
5. Future architecture recommendation.
6. Production readiness impact.
7. Final verdict:

- KEEP CURRENT REPO
- CLEAN CURRENT REPO
- CREATE thorx-app
- CREATE thorx-docs
- BOTH

IMPORTANT:

This is NOT a theoretical exercise.

I expect evidence-backed conclusions based on actual repository inspection, grep verification, build verification, and runtime validation.

Do not stop at analysis.

Determine the safest path that restores fast GitHub imports while preserving 100% of THORX functionality.